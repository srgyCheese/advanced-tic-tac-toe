import { TicTacToeMatrix } from './TicTacToeMatrix'
import { FieldTypes, Matrix, Coords, Match } from './types';

export default {
  TicTacToeMatrix,
  FieldTypes
}

export type {
  Matrix,
  Coords,
  Match
}